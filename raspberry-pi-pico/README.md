The SDK is comprehensive enough that your application code will rarely need to access the hardware registers directly.

The SDK provides higher level libraries for working with USB,multicore programming.
