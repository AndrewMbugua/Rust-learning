#include <stdio.h>
#include <stdlib.h>
#include "decimal-binary.h"

int main(){

//converts from decimal to binary,accepts ints only
//printf("Binary: "BYTE_TO_BINARY_PATTERN, BYTE_TO_BINARY(0));
//__uint32_t mask = (~(__uint32_t)(0) << 3); // possible subment mask creation

printf("%d",~(1)); //~ does a bit flip

}